import { MakeInteractiveDirective } from './make-interactive.directive';

describe('MakeInteractiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MakeInteractiveDirective();
    expect(directive).toBeTruthy();
  });
});
